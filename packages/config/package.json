{
  "name": "@trademind/config",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@trademind/shared": "*",
    "@trademind/database": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
